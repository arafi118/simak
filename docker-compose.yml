version: "3"

services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        volumes:
            - .:/var/www/html:z
        networks:
            - akubumdes-network

    nginx:
        image: nginx:alpine
        ports:
            - "${APP_PORT:-8001}:80"
        volumes:
            - .:/var/www/html:z
            - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:z
        depends_on:
            - app
        networks:
            - akubumdes-network
            - web-network

networks:
    akubumdes-network:
        driver: bridge
    web-network:
        external: true
